---
import ThemeIcon from "./ThemeIcon.astro"

let inicio = true
if (Object.keys(Astro.params).length > 0) inicio = false
console.log(Astro.params)
console.log(inicio)
---
<header class="flex justify-center">
  { !inicio && 
    <a href='/' class="fixed left-2 top-2 text-white bg-blue-700 hover:bg-blue-800 font-medium rounded-full text-sm p-2.5 text-center inline-flex items-center mr-2">
      <svg class="w-4 h-4 rotate-180" aria-hidden="true" fill="none" viewBox="0 0 14 10">
        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
      </svg>
    </a> 
  }
  <h1
    class:list={[
      "font-black uppercase text-6xl md:text-8xl text-center py-8 px-4",
      {
        "scale-100": inicio,
        "scale-75": !inicio
      }
    ]}
    style="view-transition-name: book-title"
  >
    Libros de
    <span class="block text-[36px] md:text-[58px]">programación</span>
  </h1>
  <ThemeIcon />
</header>